{
    "require": {
        "symfony/console": "^3.0",
        "symfony/process": "^3.0",
        "psr/log": "^1.0",
        "symfony/finder": "^3.0",
        "symfony/filesystem": "^3.0",
        "symfony/yaml": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "Stb\\": "src/"
        }
    },
 	"bin": [
        "bin/stb"
    ],
    "config": {
        "preferred-install": "dist"
    }
}
